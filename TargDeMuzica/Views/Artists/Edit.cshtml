@model TargDeMuzica.Models.Artist;


<h2>@ViewBag.ArtistName</h2>

<form method="post" asp-action="Edit" asp-controller="Artists" asp-route-id="@Model.ArtistID">

    <div class="form-group">
        <label asp-for="ArtistName" class="form-label">Denumire artist</label>
        <input asp-for="ArtistName" class="form-control" />
    </div>

    <button class="btn btn-success" type="submit">Modifica artistul</button>

</form>